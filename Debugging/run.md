To run langgraph-cli first install package

```
pip install langgraph-cli[inmem]
```

Then configure langgraph.json

Finally run command to in terminal to open langgraph studio

```bash
langgraph dev
```